name: Libudev
description: Bindings to `libudev.h`.
output: 'lib/src/linux/libudev.dart'
headers:
  entry-points:
    - 'linux/libudev_.h'
  include-directives: # include only these header files and necessary structs from other header files
    - '**libudev_.h'