name: Hidraw
description: Bindings to `hidraw.h`.
output: 'lib/src/linux/hidraw.dart'
headers:
  entry-points:
    - 'linux/hidraw_.h'
  include-directives: # include only these header files and necessary structs from other header files
    - '**hidraw_.h'